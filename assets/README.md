# 任务素材归档

本目录只保存任务相关图片素材，不作为 Python 代码包使用。

当前内容：

| 子目录 / 文件 | 内容 |
|---|---|
| `stone/` | crafting 任务的 9 个 stone PNG 素材 |
| `bottle/` | crafting 任务的 3 个 potion / bottle PNG 素材 |

当前 jsPsych 实验仍优先读取各实验目录下的 `materials/img/`。本目录的定位是保留一份可复用的原始/历史任务素材，避免再把旧 pygame 框架代码和素材混在 `shared/` 中。
