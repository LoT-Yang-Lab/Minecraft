nav_graph.json / trial_sequence.json — human-readable copies from export_nav_web.py
nav_embed.js — REQUIRED for index.html (includes graph + trialSequence in one script for file://)

Optional: place station images under img/ if you extend the UI; current web build uses text labels only.
